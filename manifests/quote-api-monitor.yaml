apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: quote-api-monitor
  namespace: default
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: quote-api
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
